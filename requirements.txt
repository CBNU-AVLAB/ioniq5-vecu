# IONIQ5 vECU dependencies (vECU, console and practice scripts).
# Supported on Python 3.8+. The version caps below apply only on Python 3.8/3.9.

python-can>=4.0, <4.6 ; python_version < "3.9"
python-can>=4.0 ; python_version >= "3.9"

cantools>=39, <40 ; python_version < "3.9"
cantools>=39, <41 ; python_version == "3.9"
cantools>=39 ; python_version >= "3.10"

# UDS diagnostics (ISO-TP transport + UDS client for the practice scripts)
can-isotp>=2.0, <3
udsoncan>=1.21, <2

openpyxl>=3.1
pygame>=2.5
